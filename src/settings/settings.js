module.exports = {
	async execute(interaction) {
        switch (key) {
            case player:
                playerhandler.execute()
                break;
        
            default:
                break;
        }
    }
}